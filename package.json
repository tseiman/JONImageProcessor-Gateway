{
  "name": "jonimageprocessor-gateway",
  "version": "0.1.0",
  "description": "HTTP JSON and WebSocket gateway for JONImageProcessor",
  "type": "module",
  "main": "bin/server.js",
  "scripts": {
    "start": "node bin/server.js",
    "version:write": "node scripts/update-version-info.js",
    "check": "node --check bin/*.js scripts/*.js src/*.js public/js/*.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yauzl": "^3.2.0"
  },
  "license": "MIT"
}
